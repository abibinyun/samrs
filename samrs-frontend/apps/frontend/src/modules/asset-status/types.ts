export type AssetStatus = { id: number; tenant_id: string; name: string; description?: string; created_at: string; updated_at: string };
export type AssetStatusFormInput = { name: string; description?: string };

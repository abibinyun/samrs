export type Brand = { id: number; tenant_id: string; name: string; description?: string; created_at: string; updated_at: string };
export type BrandFormInput = { name: string; description?: string };
